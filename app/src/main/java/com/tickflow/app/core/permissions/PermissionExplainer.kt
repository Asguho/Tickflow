package com.tickflow.app.core.permissions

object PermissionExplainer {
    const val WIFI_LOCATION_RATIONALE: String =
        "Tickflow needs location-related permission only to identify your configured work Wi-Fi. It does not use GPS or send Wi-Fi identifiers anywhere."

    const val NOTIFICATION_RATIONALE: String =
        "Tickflow uses notifications to keep active work tracking visible and to let you stop tracking quickly."
}
